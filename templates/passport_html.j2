<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Карточка организации</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#f0f9ff',
                            100: '#e0f2fe',
                            200: '#bae6fd',
                            300: '#7dd3fc',
                            400: '#38bdf8',
                            500: '#0ea5e9',
                            600: '#0284c7',
                            700: '#0369a1',
                            800: '#075985',
                            900: '#0c4a6e',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        .status-active {
            background-color: rgba(16, 185, 129, 0.2);
            color: #10b981;
        }
        .status-inactive {
            background-color: rgba(239, 68, 68, 0.2);
            color: #ef4444;
        }
        .card {
            transition: box-shadow 0.3s ease;
        }
        .card:hover {
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }
        .print-btn {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            background-color: #3b82f6;
            color: white;
            border: none;
            border-radius: 0.5rem;
            padding: 0.5rem 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s;
        }
        .print-btn:hover {
            background-color: #2563eb;
        }
        @media print {
            .print-btn {
                display: none;
            }
            .bg-gradient-to-r {
                background: #3b82f6 !important;
            }
            body {
                background: white;
            }
            .shadow-lg, .shadow-sm {
                box-shadow: none !important;
            }
        }
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #e5e7eb;
        }
        th {
            background-color: #f9fafb;
            font-weight: 600;
            color: #374151;
        }
        tr:hover {
            background-color: #f3f4f6;
        }
        .table-header {
            background-color: #f3f4f6;
            font-weight: bold;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-slate-50 to-slate-100 py-8 px-4">
    <button class="print-btn" onclick="window.print()">
        <i class="fas fa-print mr-2"></i> Печать
    </button>

    <div class="max-w-6xl mx-auto">
        <!-- Header -->
        <div class="bg-white rounded-t-xl shadow-lg overflow-hidden">
            <div class="bg-gradient-to-r from-blue-600 to-indigo-700 px-8 py-6 text-white">
                <div class="flex justify-between items-start">
                    <div>
                        <h1 class="text-2xl font-bold mb-2">{{ company.name_short }}</h1>
                        <p class="text-blue-100 opacity-90">{{ company.name_full }}</p>
                    </div>
                    <div class="text-right">
                        <span class="inline-flex px-3 py-1 rounded-full text-sm font-medium status-{{ 'active' if company.status == 'Действующая' else 'inactive' }}">
                            {{ company.status }}
                        </span>
                    </div>
                </div>
            </div>

            <!-- Main Info -->
            <div class="p-8">
                <!-- Requisites Table -->
                <div class="mb-8">
                    <h2 class="text-lg font-semibold text-gray-800 mb-4 flex items-center">
                        <i class="fas fa-building w-5 h-5 mr-2 text-blue-600"></i>
                        Реквизиты
                    </h2>
                    <table class="min-w-full bg-white rounded-lg overflow-hidden">
                        <thead>
                            <tr class="table-header">
                                <th>Наименование</th>
                                <th>Значение</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>ИНН</td>
                                <td>{{ company.requisites.INN }}</td>
                            </tr>
                            <tr>
                                <td>КПП</td>
                                <td>{{ company.requisites.KPP }}</td>
                            </tr>
                            <tr>
                                <td>ОГРН</td>
                                <td>{{ company.requisites.OGRN }}</td>
                            </tr>
                            <tr>
                                <td>ОКПО</td>
                                <td>{{ company.requisites.OKPO }}</td>
                            </tr>
                            <tr>
                                <td>ОКТМО</td>
                                <td>{{ company.requisites.OKTMO }}</td>
                            </tr>
                            <tr>
                                <td>ОКВЭД</td>
                                <td>{{ company.requisites.OKVED }}</td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- Address Table -->
                <div class="mb-8">
                    <h2 class="text-lg font-semibold text-gray-800 mb-4 flex items-center">
                        <i class="fas fa-map-marker-alt w-5 h-5 mr-2 text-blue-600"></i>
                        Адрес
                    </h2>
                    <table class="min-w-full bg-white rounded-lg overflow-hidden">
                        <thead>
                            <tr class="table-header">
                                <th>Наименование</th>
                                <th>Значение</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>Полный адрес</td>
                                <td>{{ company.address.full_address }}</td>
                            </tr>
                            <tr>
                                <td>Почтовый индекс</td>
                                <td>{{ company.address.postal_code }}</td>
                            </tr>
                            <tr>
                                <td>Федеральный округ</td>
                                <td>{{ company.address.federal_district }}</td>
                            </tr>
                            <tr>
                                <td>Регион</td>
                                <td>{{ company.address.region }}</td>
                            </tr>
                            <tr>
                                <td>Город</td>
                                <td>{{ company.address.city }}</td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- Management Table -->
                <div class="mb-8">
                    <h2 class="text-lg font-semibold text-gray-800 mb-4 flex items-center">
                        <i class="fas fa-user-tie w-5 h-5 mr-2 text-purple-600"></i>
                        Руководство
                    </h2>
                    <table class="min-w-full bg-white rounded-lg overflow-hidden">
                        <thead>
                            <tr class="table-header">
                                <th>ФИО</th>
                                <th>Должность</th>
                                <th>Дата начала</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for manager in company.management %}
                            <tr>
                                <td>{{ manager.name }}</td>
                                <td>{{ manager.position }}</td>
                                <td>{{ manager.start_date }}</td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>

                <!-- Footer -->
                <div class="mt-8 pt-6 border-t border-gray-200 text-center text-sm text-gray-600">
                    <p>Дата актуализации: {{ company.dates.update_date }} | Дата регистрации: {{ company.dates.registration_date }}</p>
                    <p class="mt-1">© {{ company.dates.current_year }} Информационная карточка организации</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>